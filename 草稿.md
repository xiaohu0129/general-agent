# 草稿 — general-agent 讨论记录

> 本文件为 session 讨论的归纳草稿，重点记录总结性/归纳性内容与对照表，供后续沉淀为正式文档或 OpenSpec 提案参考。

---

## 一、文档与规格现状（本轮已完成）

- 新建并归档 OpenSpec change `baseline-system-specs`，在 `openspec/specs/` 固化 **9 个能力规格（共 49 条 requirements）**：
  `chat-sse-protocol`(5) · `conversation-broker`(6) · `agent-loop`(5) · `llm-adapter`(4) · `skill-plugin`(5) · `governance-security`(7) · `web-auth-session`(6) · `observability`(6) · `deployment`(5)。
- 修订 `docs/00`（鉴权补 session、jwt fail-closed、turn_start 补 sessionId、模块/进度表）、`docs/01`（Bug 表补 B10/B11/B12、D3 鉴权、turn_lock/孤儿清洗落点）、`docs/02`（前端目录）、`README`（文档链接）。
- 验证：`openspec validate --strict` 通过；`pytest` 72 passed。

---

## 二、外部组件依赖清单

除 MySQL、Redis 外，**没有** MQ（kafka/rabbitmq/celery）、对象存储（boto3/S3）、MongoDB、ES、邮件、gRPC 等需自部署中间件。

| 组件 | 是否需单独部署 | 用途 / 触发条件 |
|---|---|---|
| **MySQL** | 必须（外部） | `agent_message` / `agent_user` / `agent_chat_session` 三表，惰性建表（aiomysql） |
| **Redis** | 可选（单实例不需要） | 多实例事件中枢 `RedisBroker`（INCR 发号 / List 环形 / Pub-Sub）+ 会话 owner 路由；留空即纯内存 |
| **外部 LLM（OpenAI 兼容端点）** | 必须接入，但非自部署 | httpx 调 `{base_url}/v1/chat/completions`；本地联调用内置 `stub_llm`(:9094) 替代 |
| **OpenTelemetry 可观测后端** | 可选 | 仅当配 `observability.otlp.endpoint` 时需部署 OTLP Collector → Jaeger/Tempo(trace) + Prometheus/Grafana(metric)；默认 console/关闭 |
| **Nginx** | 编排内自带 | frontend 容器：托管 SPA + SSE 反代，非额外中间件 |

> 文档缺口：`docs/03` §四只写"配 OTLP__ENDPOINT"，未说明背后要部署的 Collector + Jaeger/Tempo + Prometheus/Grafana。

---

## 三、历史会话是怎么保存的

**结论：消息本体只落在 MySQL 一张表 `agent_message`；LangGraph 是无状态图（不用 checkpointer）；内存/Redis 都不是消息的权威存储。** 全局扫描确认无 sqlite/shelve/pickle/写文件操作。

### 3.1 三种"会话/状态"不要混

| 数据 | 存哪 | 性质 | 重启/换设备 |
|---|---|---|---|
| 聊天消息本体（user/assistant/tool 逐行） | MySQL `agent_message` | 权威·持久 | 不丢，换设备可见 |
| 会话窗口元数据（标题/归属/时间） | MySQL `agent_chat_session` | 权威·持久 | 不丢（仅 Web 模式） |
| 用户账号/密码哈希 | MySQL `agent_user` | 权威·持久 | 不丢 |
| 登录态 token | 进程内存 `LoginSessionStore` | 瞬态 | 重启失效需重登（多实例需 Redis） |
| eventSeq/续传窗口/Pub-Sub | 内存 ring 或 Redis(可选) | 瞬态·可丢·非权威 | 重启丢、窗口外丢 |

### 3.2 表结构（mysql_client.py SCHEMA_DDL，惰性建表）

```
agent_message                                 agent_chat_session (Web)
┌─────────────────────────────┐               ┌──────────────────────────┐
│ id           BIGINT PK AI   │               │ id          BIGINT PK AI │
│ service      VARCHAR(64)  ──┼──┐            │ session_id  VARCHAR(64)U │◄─ 等于
│ env          VARCHAR(32)  ──┼──┼─ 隔离键     │ uid         VARCHAR(32)  │   agent_message
│ user_id      VARCHAR(128)──┼──┤   (复合)    │ service/env VARCHAR      │   .session_id
│ session_id   VARCHAR(128)──┼──┘            │ title       VARCHAR(128) │
│ turn_id      VARCHAR(64)    │  ◄─ 一轮归组   │ created_at/updated_at    │
│ role         VARCHAR(16)    │  user/assistant/tool     └──────────────────────────┘
│ content      MEDIUMTEXT     │
│ tool_calls   JSON NULL      │  ◄─ assistant 的工具调用
│ tool_call_id VARCHAR(64)    │  ◄─ tool 行配对用
│ created_at   DATETIME(3)    │
│ INDEX idx_session(service,env,user_id,session_id,id) │
└─────────────────────────────┘
```

### 3.3 写入链路（一轮对话）

```
run_turn(session_id, turn_id, user_message)
  1. yield turn_start(sessionId)              ← 先于任何 I/O
  2. load_messages(limit=500, ORDER BY id DESC 再 reverse)
       → _drop_unanswered_tool_calls（洗末尾孤儿）
       → _trim_history(max_context_tokens=24000)
  3. append_message(role="user", ...)          ← 用户消息落库
  4. agent.astream_events(...) 跑 LangGraph    ← 收集 assistant/tool
  5. _drop_unanswered_tool_calls(new_messages) ← 落库前再洗一次
       → append assistant/tool 消息
  6. yield turn_end
  7. (producer finally, session 模式) chat_sessions.touch → 更新 updated_at
```

### 3.4 读取链路（两个消费者）

| 消费者 | 方法 | 查询 | 用途 |
|---|---|---|---|
| LLM 记忆（每轮重载） | `load_messages` | `ORDER BY id DESC LIMIT 500` → reverse → 清洗 → trim | 喂给图的上下文 |
| 前端回放（刷新/切会话） | `load_web_messages` | `ORDER BY id ASC`（**全量无 limit**） | UI 气泡/工具卡 |
| 会话列表 | `chat_session_store.list_for_user` | `ORDER BY updated_at DESC LIMIT 50` | 侧边栏 |

### 3.5 隔离键在两种模式的来历

| 维度 | API 模式(disabled/api_key) | Web 模式(session) |
|---|---|---|
| user | `x-user` 头（默认 anonymous） | 登录态 **uid**（uuid4 hex） |
| service | `x-service` 头（默认 default） | 固定配置 web.service("web") |
| env | `x-env` 头（默认 dev） | 固定配置 web.env("dev") |
| session_id | 传入 或 `session_key(svc,env,user)`（一身一会话） | 传入(归属校验) 或新建 uuid4（一 uid 多会话） |

所有 SQL WHERE 都带 `service AND env AND user_id AND session_id`；Web 模式再叠加 `agent_chat_session.uid` 归属校验（越权 404）。

---

## 四、主流 Agent 历史会话管理调研

### 4.1 先拆：两种"记忆"

| | ① 工作记忆 / 对话转录 | ② 长期 / 语义记忆 (RAG) |
|---|---|---|
| 回答的问题 | "这一轮之前我们说了啥" | "跨所有会话，之前提过/相关的" |
| 访问方式 | 有序、追加、按会话扫（recency） | embedding 相似度近邻（relevance） |
| 要求 | 顺序、事务、按 session 范围、不丢 | 向量近邻 / 全文检索 |
| 存储 | 关系库 / 文档库 / KV | 向量库 / 全文引擎 |

> "MySQL 不行"的直觉，多半是把 ② 的需求错套到了 ① 上。

### 4.2 主流方案对照

| 项目/框架 | 对话转录存哪 | 语义/RAG 存哪 | 热态/流 |
|---|---|---|---|
| LangGraph（官方生产） | **Postgres**（Saver/Store，thread_id=会话）；dev SQLite/内存 | Postgres Store + pgvector | Redis saver 可选 |
| OpenAI Assistants/Responses | 厂商托管 threads/messages（黑盒） | 独立 vector_store | — |
| Vercel AI SDK | 无状态自带 DB，范例多 **Postgres**/Redis | 自接 | Redis/KV |
| Dify | **PostgreSQL**（会话/消息/账号） | Weaviate/Qdrant/Milvus/pgvector | Redis |
| Letta / MemGPT | **Postgres**（state + core memory） | archival 走向量（pgvector/外部） | — |
| Mem0 | **Postgres/SQLite**（memory 条目） | Qdrant/Chroma/pgvector | Redis |
| Zep | **Postgres**（消息）+ 时序知识图谱 | pgvector + Graphiti(Neo4j) | — |
| 本地单机（AutoGPT 等） | **SQLite** | 本地 Chroma 等 | — |

**规律**：对话转录层主流几乎都是关系库（且压倒性是 **Postgres**），不是向量库；向量库只在 RAG/长期记忆层出现；文件/上传丢对象存储（S3/OSS）。

**为什么 Postgres 多于 MySQL**：不是性能，是生态——`JSONB`、**pgvector 一库干转录+向量**、`LISTEN/NOTIFY`、LangChain/LangGraph 官方 saver 优先 Postgres。

### 4.3 主流存储分层

```
浏览器/客户端 ←─ 回放(分页)
      │
┌─────▼─────────┐
│ 热态/实时 Redis│  seq、ring、Pub/Sub、缓存、队列
└─────┬─────────┘
┌─────▼─────────────────────┐
│ 对话转录 (关系库)           │  有序消息、会话、账号
│ Postgres/MySQL/SQLite     │  ← 本项目用 MySQL
└─────┬─────────────────────┘
      │（做 RAG 时旁路）
┌─────▼─────────┐      ┌──────────────────────┐
│ 向量/全文 RAG  │      │ 对象存储 (S3/OSS)     │
│ pgvector/Qdrant│      │ 上传文件、超长工具输出 │
│ /Milvus/...   │      └──────────────────────┘
└───────────────┘
```

### 4.4 MySQL 在本项目的性能分析

**访问模式**：追加写 + 按会话有序范围读，人类对话速度。

```
一轮 DB 操作：
  读: 1× SELECT ... WHERE(会话) ORDER BY id DESC LIMIT 500  → 索引反向范围扫，扫满即停
  写: 1× INSERT(user) + N× INSERT(assistant/tool)  N≈1~10
  → 每轮 DB 耗时 ms 级；对比 LLM 一轮 1000~30000ms，DB 占整轮延迟 ≈ 0.01%~0.1%
容量粗算：1000 活跃用户、每人 5 秒一轮 = 200 轮/秒 ≈ 2000 写/秒 + 200 读/秒 → MySQL 轻松
         按会话索引查最近 500 行与表总量无关，大表不影响单会话尾读
```

**结论：转录场景下"MySQL 不可行"大概率是伪命题；真正先撞墙的不是数据库。**

**瓶颈排序（先撞 → 后撞）**：

| 排序 | 瓶颈 | 说明 |
|---|---|---|
| ① | LLM 上下文/token 成本 | 每轮重发历史，500 条可能几万~几十万 token，慢+贵+撞上限；靠 trim/summarize/RAG，与引擎无关 |
| ② | 全量回放无分页 | `load_web_messages` 无 LIMIT，超长会话刷新一次返回几千行（真实缺口） |
| ③ | 单实例约束 | 内存 Broker + 内存登录态，不能多副本（比 DB 更早卡扩展）；MySQL 外置反而是助力 |
| ④ | 连接池偏小 | aiomysql `pool_size=5`，高并发可能排队（操作 ms 级，影响有限） |
| ⑤ | MySQL 本身 | 此 workload 下几乎不是瓶颈 |

**MySQL 短板（诚实版）**：

| 说法 | 真相 |
|---|---|
| "关系库存对话不专业" | ❌ 主流转录层都用关系库 |
| "查询会慢" | ❌ 按会话索引尾读 500 行与表总量无关 |
| "JSON 存不了工具调用" | ❌ MySQL 8 有原生 JSON（本项目用 MEDIUMTEXT+json.dumps，可改原生 JSON 列） |
| "要做语义搜索/跨会话记忆" | ✅ **真短板**——向量近邻弱，届时加向量库或 pgvector |
| "超长工具输出/文件塞 content" | ⚠️ 真问题——大 payload 撑大行，应转对象存储、库存引用 |
| "前端全量回放" | ⚠️ 应用层缺分页（keyset `WHERE id>? LIMIT ?`，现有索引直接支持），非 MySQL 缺陷 |

**建议**：转录层 MySQL 可行，无为性能换库的必要；若近期做 M9 RAG，Postgres+pgvector 可让"转录+向量"收敛一库（生态理由，非性能理由）。更该先补：回放分页、原生 JSON 列/大 payload 外置、pool_size 调优、summarize/RAG。

---

## 五、项目架构与已解决问题

### 5.1 运行时组件全景

```
浏览器(React SPA, front/) ──cookie(HttpOnly)+SSE──► Nginx(容器: 静态资源 + /chat /stream 反代关缓冲)
        │
        ▼
┌──────────────────────── FastAPI app (app.py) ────────────────────────┐
│ 路由: health / auth_routes(/auth/*) / chat(POST /chat)               │
│       stream(GET /stream) / sessions(/sessions*) / notify            │
│ app.state: model(OpenAICompatibleModel→LLM) / skill_registry /        │
│   services{} / broker(可换 RedisBroker) / turn_locks / inflight /    │
│   rate_limiter / login_sessions / user_store / chat_sessions /       │
│   message_store → MySQL(agent_user / agent_chat_session / agent_message)
│ 横切: observability(OTel traces/metrics) + structlog(脱敏)            │
└──────────────┬───────────────────────────────────┬───────────────────┘
        ▼                                        ▼
  外部 MySQL(必须)                  Redis(可选)/LLM(必须,或 stub:9094)/OTLP(可选)
```

### 5.2 三条核心链路

- **A. Web 对话 `POST /chat`**：governance_dep(cookie→Identity→env 白名单→限流) → 会话归属校验/预生成 → Skill 过滤建 agent → broker.subscribe → spawn producer（**turn_locks 锁内**：建会话行 + run_turn）→ consumer SSE（15s 心跳、只转发本轮 turnId、turn_end/error 收尾）；客户端断开 producer 继续跑完落库。
- **B. 持久通道 `GET /stream`**：归属校验 → Last-Event-ID（查询参数优先于头）→ 先 replay ring 中 seq>last → 再转 live（按 max_replayed_seq 去重）→ 全程心跳，下发会话全部事件。
- **C. 异步通知 `POST /internal/notify`**：api_key 鉴权 → publish_notification → 在线 fanout / 离线入 ring 待重放。

### 5.3 已解决的问题

**① 长任务不稳定（60% → 99%+）**

| 问题 | 方案 |
|---|---|
| SSE 空闲被掐断 | 15s 心跳注释行 |
| 断线丢事件 | eventSeq + ring buffer + Last-Event-ID 重放 |
| 连接断执行也停 | producer/consumer 解耦，producer 不随断开取消 |
| 慢消费者拖垮生产 | put_nowait 背压丢弃，ring 全量保留可重放 |
| producer 被 GC | 同步 inflight.add（B9） |
| 停止后重发历史交错 | TurnLockRegistry 引用计数串行化（B12/E9） |
| 极早断开留空孤儿会话 | 会话行延后到 producer 锁内建（B12/E9） |
| 多实例事件路由 | RedisBroker：INCR/List 环形/Pub-Sub（已实现，接线即可） |

**② 安全与多租户隔离**

| 问题 | 方案 |
|---|---|
| 无鉴权任意访问 | 多模式 disabled/api_key/session（B4） |
| 误配 jwt 静默放行 | 占位 fail-closed 抛 500 CONFIG（B11/E8） |
| 跨用户/租户读数据 | 身份键进 SQL WHERE；Web 用 uid；越权 404 |
| 测试 env 调生产工具 | env 白名单 + Skill allowed_envs 注册期过滤 |
| 凭证泄露日志 | structlog 递归脱敏；api_key 仅内存 |
| 滥用/撞库 | 令牌桶限流 429；登录失败 5 次/10 分钟锁定 |
| Web 登录安全 | PBKDF2、HttpOnly+SameSite cookie、即时吊销、防用户枚举、CORS 禁 `*` |

**③ 可观测性**：OTel span 树（server→run_turn→load_history/agent_graph→llm_call/tool_call→append_messages）；7 项低基数 metric（高基数 ID 只进 span 不进 metric）；structlog 注入 trace_id/span_id；双收 propagator（traceparent 优先 + X-Trace-Id 兜底）；severity 映射，max_tool_rounds 不算 ERROR span。

**④ Agent/LLM 工程化**

| 问题 | 方案 |
|---|---|
| 绑定单一模型厂商 | OpenAI 兼容适配层 + 本地 stub |
| 工具无限死循环 | max_tool_rounds → recursion_limit=2n+1，受控 turn_end |
| 上下文膨胀 | token 预算 trim 且保 tool 配对（B2） |
| 工具异常崩整轮 | ToolNode(handle_tool_errors)，错误转 ToolMessage 给模型 |
| 递归中断遗留孤儿 tool_calls 致永久 400 | _drop_unanswered_tool_calls 载入后+落库前双洗（B10/E7） |
| 版本升级兼容坑 | langchain-core 1.x 空分片、aiomysql 多语句、UTF-8 写文件（错题本 E1/E5/E6/B8） |

### 5.4 还没解决的缺口

- 前端回放全量无分页（`load_web_messages` 无 LIMIT）
- `context_strategy=summarize` 预留未实现（token 成本随历史线性涨）
- Redis 版登录态未实现 → 多实例仍卡在这
- M9 RAG / 跨会话长期记忆未做（向量层缺失）
- 大工具输出/上传文件未外置对象存储（塞 MEDIUMTEXT）
- 无 DB migration 工具（加列需手动）；pool_size=5 偏小
- OTLP 后端需自部署，compose 默认关闭

---

## 六、工作记忆 vs 长期记忆：怎么区分、放哪个库

**核心：不是"两类数据"，是"两种召回机制"；存储引擎是结果。判据按"访问方式和职责"，不按数据类型。**

```
时间轴（recency/order）              语义轴（relevance）
"最近说了啥，按顺序"                 "跟眼前这句话意思相关的，跨时间"
按 session_id + 时间倒序             embedding 近邻 + top-k
确定性、要全、要有序                  概率性、只要最相关几条
→ 关系库(MySQL) 主场                 → 向量库(Milvus) 主场
        └──────────────┬─────────────┘
                       ▼
            都受 token 预算约束，拼成有界上下文
```

同一条信息可两边都在（如"我习惯中文回复"：逐字留转录 + 抽成偏好入长期）。是"一条原始数据 + 零到多条派生抽取"。

### 6.1 三个判据问题

- **Q1** 将来是否需要"逐字、按顺序"重放这段对话？ → 是 → 工作记忆（MySQL，不可变转录日志）
- **Q2** 它会不会在【未来另一个会话】里被"按意思"而非"按时间"想起？ → 是 → 长期记忆（Milvus）
- **Q3** 换 embedding 模型 / 向量库丢了，系统还能活吗？ → 能（可从 MySQL 重跑抽取重建）→ 向量库是**派生索引**，不是事实源

### 6.2 事实源 vs 派生索引（架构上最重要）

```
MySQL (agent_message) = 事实源 (system of record)，不可变、权威、不能丢
Milvus                = 派生索引 (derived index)，可丢弃、可全量重建
                         （类似搜索引擎倒排索引之于数据库）
```

向量库定位成"派生缓存/索引"，一致性压力大减——脏了/丢了从转录重跑即可。

### 6.3 内容分类清单（决定放哪）

| 内容 | 工作记忆 MySQL | 长期记忆 Milvus | 说明 |
|---|:---:|:---:|---|
| 用户逐字消息 | ✅ 必存 | ◐ 可选* | 转录永远落 MySQL |
| 助手回复 / tool 结果 | ✅ 必存 | ✗ | 逐字回放用，不单独入向量 |
| 本轮工具调用状态/中间结果 | ✅ | ✗ | 瞬态任务态，跨会话无意义 |
| **用户偏好**（"用中文""简短点"） | ✅(原文) | ✅ **抽取** | 跨会话稳定，最该进长期 |
| **用户画像/身份**（"在做 X 项目"） | ✅ | ✅ **抽取** | profile 类 |
| **事实/实体**（"订单 A123 已退款"） | ✅ | ✅ **抽取** | 可能被推翻，需可更新 |
| **跨会话经验/教训** | ✅ | ✅ 摘要 | 反馈类，指导未来行为 |
| **历史会话摘要**(episodic) | ✗ | ✅ **摘要** | 每 session 结束生成一段 |
| 外部文档/RAG 语料(PDF/知识库) | ✗（除非对话引用） | ✅ **独立 namespace** | "外部知识"≠"从对话学的"，别混 |
| 登录态/session token | ✗ | ✗ | 内存/Redis，与记忆无关 |

> *"可选"= 是否把原始消息也 embed 用于跨会话"搜历史对话"。主流（Mem0/Letta）倾向**只嵌摘要/事实**（写时贵、检索干净、库小）；跨会话翻聊天记录需求强时再嵌原始消息。

### 6.4 三层记忆模型（MemGPT/Letta 术语）

```
┌─────────────────────────────────────────────┐
│ Core 核心记忆  始终在 context（人设/当前工作） │ ← system prompt + 少量
├─────────────────────────────────────────────┤
│ Recall 回忆    完整转录，可搜索               │ ← MySQL（本项目已有）
│   按时间重放 = 工作记忆窗口                    │
│   按语义搜索 = 把转录也 embed（可选）          │
├─────────────────────────────────────────────┤
│ Archival 归档  抽出的事实/偏好/摘要，向量检索  │ ← Milvus（待建）
└─────────────────────────────────────────────┘
```
本项目现状：只有 Recall（且仅时间重放，无语义搜索）；待补 Archival；Core 目前是固定 `system_prompt`。

### 6.5 读写链路

```
【写：每轮/每会话后，异步抽取】
 turn_end（现有 broker 事件/producer 是天然钩子）
   → 抽取管线（额外一次 LLM 调用，不阻塞对话）
       输入本轮/本会话转录，输出候选记忆 [{type, content, 实体...}]
       过滤：跨会话相关？稳定非瞬态？自包含？（不是每句都值得记）
   → 去重/冲突消解（最难）：先向量检索有无相似记忆
       重复→忽略 / 被推翻→旧条目标 expired 墓碑(或 update) / 全新→插入
   → upsert Milvus：{embedding, uid(隔离), memory_type, content,
                     source_session, source_msg_ids, created_at, status}

【读：下一轮两路召回后拼接】
 用户新消息
   ├─ MySQL: load 最近 N 条（recency，token 预算内，已做 trim）
   ├─ embed(query) → Milvus 检索 WHERE uid=<本人> top-k（relevance）
   │      └─ 过相似度阈值（太低不塞，噪声比不塞更糟）
   → 组装 prompt：[system/core] + [相关长期记忆] + [最近工作记忆] + [本轮输入]
   → LLM
```

### 6.6 真正难的四件事（不是"接 Milvus"）

| 难点 | 为什么 | 常见做法 |
|---|---|---|
| 抽取时机与成本 | 每轮抽 = 每轮多一次 LLM 调用，慢且贵 | turn_end 后异步；会话末抽摘要+关键事实；检测"值得记"才抽 |
| 去重/冲突 | 朴素插入堆满矛盾旧事实（"喜欢X"/"不喜欢X"都被召回） | 写前语义查重，ADD/UPDATE/DELETE/IGNORE 四态（Mem0 核心） |
| 检索注入的度 | 塞太多/不相关反而误导模型、浪费 token | uid 强过滤 + top-k + 相似度阈值 + 按 memory_type 分组注入 |
| 多租户隔离 | 同 MySQL 红线 | Milvus 用 uid 标量过滤或 partition，绝不跨用户召回 |

> 关键提醒：**上 Milvus 也不会让工作记忆变长**——token 预算和 trim 依然要做。长期记忆解决"跨会话记得你是谁/定过什么"，不解决"这一轮塞下整段历史"，两者独立。

### 6.7 本项目对接点（仅位置，未改代码）

- 写钩子：`runner.run_turn` 的 turn_end 处，或监听 broker turn_end 事件，起异步 memory-extraction 任务（复用 producer/inflight 模式）。
- 读注入：`runner.py` `_trim_history` 拼历史前，加一路"长期记忆召回"，作为额外 context 段（或 system prompt 的"已知信息"区）。
- 隔离：`Identity.user`（Web 模式即 uid）作向量检索过滤条件，沿用现有 service/env/user 隔离。
- config：新增 `memory:` 配置块（provider、top_k、阈值、抽取开关），风格对齐 `llm:`/`redis:`。
- 事实源不变：`agent_message` 照旧；Milvus 是可重建派生层。

---

## 七、待办 / 可沉淀方向（待定）

- [ ] （可选）`docs/03` 补"可选可观测后端部署"小节（Collector + Jaeger/Tempo + Prometheus/Grafana）。
- [ ] （可选）补 `docs/04-架构总览与问题清单.md`（第五部分内容）。
- [ ] （可选）补"记忆与存储选型"设计文档（第四、六部分）。
- [ ] （需走 OpenSpec change + 退出 explore）真实缺口提案：回放分页、大 payload 外置对象存储、summarize、长期记忆(Milvus)、Redis 版登录态。

---

## 八、openspec 和 superpowers 的使用流程

> 本项目工作约定（见 AGENTS.md）：**OpenSpec 管方案、superpowers 管执行**，两套流程分工不重复。
> OpenSpec 技能回答"做什么、做到哪了"；superpowers 技能回答"怎么写代码、怎么验证没骗人"。

### 8.1 角色分工

- **OpenSpec**：变更的全生命周期管理——探索、定稿（proposal/design/specs/tasks）、进度追踪、spec 合并、归档。现场全部持久化在 `openspec/changes/<change-name>/` 目录。
- **superpowers**：执行层方法论——TDD、派子代理、系统化调试、完成前验证、code review 等"怎么做"的纪律。
- **去重规则**：走 OpenSpec 的变更**不触发** superpowers 的 `brainstorming`（explore 覆盖）和 `writing-plans`（proposal/design/tasks 覆盖）；执行层技能照常使用。

### 8.2 正式变更（新功能/架构调整/涉及 spec）完整流程

| 步骤 | 键入命令 | 阶段产出 / 动作 | 用到的技能及作用 |
|---|---|---|---|
| 0 | （自动） | 会话开场，确立"动作前先查技能" | superpowers·`using-superpowers`：元规则 |
| 1 | `/opsx-explore <需求描述>` | 探索讨论：读代码、画图、澄清边界/防护/验证方案；**只读不写实现** | openspec·`openspec-explore`：探索姿态，确认范围后可 scaffold change 并记录思考（brainstorming 去重） |
| 2 | `/opsx-propose <change-name>` | 一次性生成四工件：`proposal.md`（做什么/为什么）、`design.md`（怎么做/边界）、`specs/<cap>/spec.md`（delta 行为规格）、`tasks.md`（任务清单）；**生成后停下，不顺手写码** | openspec·`openspec-propose`：建 change、生成工件、`openspec validate` 校验格式（writing-plans 去重） |
| 3 | `/opsx-apply <change-name>`（或说"开始实现"） | 读 tasks.md → **逐任务派独立 subagent**（子代理内 TDD：先写失败测试→跑红→写实现→跑绿）→ 主会话审查 diff、勾选 `- [x]`、串联下一任务；无依赖任务并行 | openspec·`openspec-apply-change`：管进度、读上下文、勾选、遇歧义暂停<br>superpowers·`subagent-driven-development`：主执行模式（implementer + task-reviewer 子代理）<br>superpowers·`test-driven-development`：子代理内红绿循环<br>superpowers·`dispatching-parallel-agents`：独立任务并行<br>superpowers·`using-git-worktrees`：需要隔离工作区时（可选） |
| 3.5（回环） | `/opsx-update <change-name>` | 实现中发现方案问题时，**暂停写码**修订 proposal/design/specs/tasks 保持一致，再回步骤 3 | openspec·`openspec-update-change`：方案变更同步进文档（文档与代码同步）<br>superpowers·`systematic-debugging`：遇 bug/测试失败时先复现、根因追踪再修复；bug 按全局规则记入 `docs/` 错题文档 |
| 4 | （无斜杠命令，键入提示词，例如）`"任务都完成了，按完成前校验流程走一遍：openspec validate、全量测试、lint、typecheck，然后派 code review 子代理对照 spec 逐条审查，把真实输出贴给我，没跑完不许说完成"` | 校验三件事：① `openspec validate --change <name>`（spec 结构）② 全量测试 + lint + typecheck ③ 派 review 子代理对照 spec 逐条审查 | superpowers·`verification-before-completion`：铁律——没跑命令看到真实输出（退出码/失败数）不许声称完成；子代理汇报不算数，主会话独立复核<br>superpowers·`requesting-code-review`：派 code-reviewer 审查<br>superpowers·`receiving-code-review`：对 review 意见技术核实，不盲从不敷衍 |
| 5 | `/opsx-archive <change-name>` | ① 检查 tasks 全部 `[x]` ② delta spec **智能合并进主 specs**（`openspec/specs/`）③ change 目录移到 `changes/archive/YYYY-MM-DD-<name>/` | openspec·`openspec-archive-change`：完成度检查→内联同步→移目录<br>openspec·`openspec-sync-specs`：delta→主 spec 合并（归档内联跑；也可 `/opsx-sync` 单独跑）<br>superpowers·`finishing-a-development-branch`：分支收尾（merge/PR 需用户明确指示） |

**关键顺序**：必须**先实现+校验全部完成，再归档**。归档会移走 change 目录并合并 specs；先归档会丢失 tasks 进度追踪和 delta 上下文。archive 流程本身也以"任务全部完成"为正常前提（未完成会警告并要求确认）。

### 8.3 小改动旁路（不涉及 spec）

配置值、错别字、一次性脚本等琐碎修改**不走 8.2**，直接对 agent 说需求，内部走轻量链：

`brainstorming`（一句话确认意图）→ `test-driven-development`（适用时）→ `verification-before-completion`（跑完验证再报完成）。不创建 OpenSpec change。

### 8.4 校验在哪里执行

**在 opencode 会话内由 agent 执行**，不需要用户新开终端：

- verification-before-completion 要求 agent 自己跑命令、读完整输出、看退出码，才能做完成声明；用户在外部终端跑完 agent 看不到输出，证据链不闭环。
- openspec 技能头部声明 `allowed-tools: Bash(openspec:*)`，`openspec validate/status` 等本就是设计为 agent 在流程中直接调用。
- 用户自己开终端仅适合：长期运行的服务（dev server/DB）、交互式调试、私下抽查；但正式"完成门禁"仍应让 agent 在会话内重跑留证。

### 8.5 保存现场与跨 session 恢复

**原理**：现场 = `openspec/changes/<name>/` 目录（工件）+ tasks.md 的 `- [x]` 勾选（进度）+ 工作区代码。会话可丢，文件不丢；**唯一会丢的是"只聊过没落盘"的内容**。

结束会话/context 快满前：
1. 让 agent 把已达成的决策、悬而未决问题、下一步第一件事写进工件（explore 阶段允许记录思考到 change；实现阶段更新 tasks.md 勾选并在 design.md 备注卡点）；
2. 代码改动留工作区即可（agent 不会自动 git commit；`git stash`/WIP commit 需用户明确指示）；
3. 记住 change 名字——恢复的唯一钥匙。

新 session 恢复（先 `openspec list` 看待办 change）：

| 中断在哪 | 恢复命令 | 效果 |
|---|---|---|
| 探索到一半 | `/opsx-explore <name>` | 读已有工件继续讨论 |
| 定稿没做完 | `/opsx-propose <name>` | 识别已有工件，补齐缺件（`openspec status` 显示缺哪个） |
| 实现到一半 | `/opsx-apply <name>` | 自动显示"N/M tasks complete"，从第一个未勾选项继续 |
| 方案改了一半 | `/opsx-update <name>` | 工件重新对齐后再 apply |

新 session 无聊天记忆，agent 靠：读 change 工件 → `openspec status --json` → tasks.md 勾选 → `git status/diff` 重建现场；用户补一句"昨天做到 XX、卡在 YY"（依据就是落盘备注）。

### 8.6 手动 interrupt 后的处理

先判断中断点有没有副作用：

- **中断在纯思考/打字（未调工具）**：零副作用，直接重新输入指令即可。
- **中断在工具执行中**（写文件/跑命令/派子代理）：磁盘可能半完成，**不要立刻继续**。先让 agent 只读检查：
  `git status && git diff`、`openspec status --change <name> --json`、`openspec list`，报告现场后再决定。

| 中断点 | 现场风险 | 处理 |
|---|---|---|
| 编辑文件中 | 文件写一半 | 看 diff，修补或 `git checkout -- <file>` 还原（需用户确认） |
| 跑测试/build 中 | 命令被杀，无副作用 | 重跑 |
| 子代理实现任务中 | 代码已写入但未审查、tasks 未勾选 | 审查 diff 产物；完整则勾选，不完整则重做该任务（TDD 下测试已在，重做基本幂等） |
| `/opsx-update` 改工件中 | 工件改一半互不一致 | 重跑 `/opsx-update <name>` 对齐 |
| `/opsx-archive` 归档中（最需小心） | spec 合并一半 / 目录正在移动 | 检查三处：主 specs 是否已合入、change 在 `changes/` 还是 `archive/`、有无重名残留；确认后再继续同步或重新归档 |

中断后第一句话建议："你刚才被中断了，先用只读命令检查工作区和 change 状态，报告现场，不要动手改。"
兜底安全网：agent 不自动 commit，`git diff` 永远能看到全部未提交改动。
